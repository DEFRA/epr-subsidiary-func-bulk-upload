version: '3.8'

services:
  app:
    build:
      context: src
      dockerfile: EPR.SubsidiaryBulkUpload.Function/Dockerfile
      platforms:
        - linux/amd64
    environment:
      FUNCTIONS_WORKER_RUNTIME: "dotnet-isolated"
      AzureWebJobsStorage: "UseDevelopmentStorage=true"
      HOST: "localhost"
      Redis__ConnectionString: "localhost:6379,abortConnect=false,connectTimeout=1500"
    ports:
      - "7071:80"
    healthcheck:
      test: ["CMD-SHELL", "timeout 2 bash -c '</dev/tcp/localhost/80' || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 5s
